# Core PyTorch + video I/O
#torch
#torchvision
#torchaudio

# Image + audio handling
Pillow
soundfile
numpy

# Video writing fallback
imageio

# OpenCV (if needed by KeySync’s scripts)
opencv-python

# ffmpeg-python (KeySync may call ffmpeg via Python)
ffmpeg-python

# For the Python wrapper (infer.py)—no extra deps beyond stdlib & above
